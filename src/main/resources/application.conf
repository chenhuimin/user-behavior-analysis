#mongodb related settings
mongodb-settings {
  host = "172.16.91.36"
  port = 22117
  inputDatabase = user-behavior
  inputCollection = behavior
  outputDatabase = behavior-analysis-result
  outputCollection = behavior-statistics
}
job-settings {
  #job execution interval, unit: minute
  executionInterval = 60
}
#tachyon related settings
tachyon-settings {
  host = "node3.laanto.com"
  port = 19998
  basePath = /data/raw/userBehaviors/
}
